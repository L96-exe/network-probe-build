name: Build Android APK

on:
  wor**low_dispatch:

jobs**  build:
    runs-on: ubuntu-late**

    steps:
      - name: Checko**
        uses: actions/checkout@v**
      - name: Set up Java
      **uses**actions/setup-java@v4
        wit**
          distribution: temurin
**        java-version: "17"

     ** name: Set up Gradle
        uses**gradle/actions/setup-gradle@v4
  **    with:
          gradle-versio** "8.9"

      - name: Set up Andr**d SDK
        uses: android-actio**/setup-android@v3

      - name: **stall SDK packages
        run**sdkmanager "platforms;android-35"**build-tools;35.0.0"

      - name**Build APK
        run: gradle :ap**assembleDebug --stacktrace

     ** name: Upload APK
        uses: a**ions/upload-artifact@v4
        w**h:
          name: NetworkProbe-d**ug-apk
          path: app/build/**tputs/apk/debug/app-debug.apk
   **     if-no-files-found: error
```**
